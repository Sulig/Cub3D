# Cub3D TODO LIST
This things needs revision for merge work

## GENERAL THINGS
- [] Add the new files & directories to Makefile
- [] Clean and redistribuite the code into files
- [] Free all memory reserved
- [] Norminette (Last thing)

## FILE && MAP ERROR HANDLING

- [X] Error file.type atached to program display a custom error
- [X] Error in permisions display a error

- [X] In reading on file, if something is missing (like one character, one texture, etc..) display a custom error
	* [X]! The textures might be in these format: .png (".png" is the only image format suported in MLX42) (is doed in start)

- [X] The same as previous, but in case of duplicate data (** Only with player spawning and double character)
	* If is the same path texture, but into differrent wall asisignment, don't count that as error (i will set the same texture into thoose wall orientation)
	-- like:

	WE ./assets/textures/xpm/error-tile.xpm
	EA ./assets/textures/xpm/error-tile.xpm

- [] Check the numbers introduced in floor and ceiling
	- This should be in 0-255 range

- [] Check if map is surrounded by walls (Can I ask that the tabs count as 4 spaces within the map?)

	 1
	1N1		// This would be an OK map
	 1

	[] ** Spaces/Tabs inside map it should be considerated as '0'

- [] Check if the data is valid (if display an error use `print_other_err(char *err)`)

		-----------------------
***

## MAP PARSER DATA -
** Details: SEE THE "cub_structs.h" file

- [X] Save the max_width && height of map
	** Get the max line strlenght for the width!

- [X] Map parsed into a 2D array
- [X] The path to textures shall be saved into a *char

- [X] The 3 values of color of Ceiling && Floor, should be saved into an int array
	* Save a tmp with the line obtained with gnl
	* Use ft_split(tmp, ',') to save into a tmp array
	* Use atoi and save the values into a int array
	(for example: *c_flr, c_sky[0] = 225, c_sky[1] = 224, c_sky[2] = 255)

- [X] The start position of player && orientation
	* Orientation can be literally N, S, W or E
	* [X] Cast orientation into the ply.pa (Player Angle)

		-------------------------
***

## RAYCASTING

- [] MAKE IT WORK!

- [X] Save the colors in t_game:
	int32_t		c_flr;
	int32_t		c_sky;

- [X] Set Player orientation
	~ pa = 0	=> Look right
	~ pa = PI	=> look left
	~ pa = P2	=> look down
	~ pa = P3	=> look up

- [X] Set player position
- [X] Save the fnc utilities in a file
- [] Paint the BG with no segfault
- [] Start Raycasting with try-me file
