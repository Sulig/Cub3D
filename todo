* Issue p0:
 The "A" && "D" commands are not working as intended

	** DONE!
***

* Convert Raycast into 3D:
	** Needs improvement but is also done!
***

* Apply textures
	IN PROGRESS ~

	** Need to think how to apply the correct ttexture to the wall
	*~ I have implemented a little if - elseif - else (Brujula)
		but the NO && SO is not working
***

> [!WARNING]\
> Add exit(EXIT_FAILURE) on a error (*Probably ya doned... but still revision)

***
